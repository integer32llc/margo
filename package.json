{
  "name": "margo",
  "version": "0.0.0",
  "private": true,
  "source": "ui/ui.html",
  "targets": {
    "default": {
      "distDir": "ui/dist",
      "publicUrl": "./assets",
      "engines": {
        "node": ">=20",
        "pnpm": ">=9",
        "browsers": "since 2024"
      }
    }
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.9",
    "parcel": "^2.12.0",
    "postcss": "^8.4.38",
    "prettier": "^3.2.5",
    "tailwindcss": "^4.0.9",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build",
    "check": "tsc --noEmit",
    "fmt": "prettier . --write",
    "fmt:check": "prettier . --check"
  }
}
